
export enum EntityType {
  UNKNOWN = 0,
  PERSON = 1,
  MESSAGE = 2,
  RESOURCE = 3,
}

export enum PoiType {
  UNKNOWN = 0,
  LANDMARK = 1,
  MERCHANT = 2,
  USER = 3
}

export enum UserUnlocks {
  TELEPORT_KNOWLEDGE = 1
}
